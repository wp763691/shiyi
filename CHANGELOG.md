# Changelog

## v1.0.9 (2026-09-13)

- 修复终端标签与会话列表名称不一致：标签名改用与列表同一来源，改名时一次写入该会话的全部别名 key，任一侧改名另一侧立即同步（历史单边别名自动对齐）
- 演示模式的会话别名改为内存存储，不再写入用户真实的 `~/.shiyi/session-names.json`

## v1.0.8 (2026-09-13)

- 上下文快满时的交接流程：达到 90% 自动弹确认框，可就地执行 `/compact`，或"开新会话 + 交接摘要"（新会话先读旧记录再输出交接摘要）
- 上下文徽标可点击，随时调出同一个交接对话框；关掉后本会话不再自动弹出
- 应用图标与页面左上角 Logo 换为「薄荷汽水」配色，图标改为参数化主题（sunrise / mint / sky）
- 新增 `app/macos/make_icon.sh`：一条命令同时更新 AppIcon.icns 与 public/brand.png

## v1.0.7 (2026-09-13)

- 终端文本输入重构为单通道（input/compositionend），彻底修复中文输入法下"按两次/一次多出"问题
- 新增会话上下文占用徽标（真实 usage，80%/90% 阈值提示，行背景同步着色）
- 会话目录采用最近写入 cwd；失效目录不再强行 cd
- 抽屉头部重设计、多连接方治理、内存显示与释放空闲会话

## v1.0.6 (2026-09-13)

- 修复按键重复输入（可打印字符直通补 preventDefault）
- 会话目录采用最近写入 cwd（目录改名自动跟随），失效目录不再强行 cd
- 抽屉头部重设计：标题 + 内存 + ＋ + ⋯
- 多连接方提示与「断开其他连接」；终端默认不接管滚轮
- 内存占用显示与「释放空闲内存」

## v1.0.5 (2026-09-13)

- 会话角标改为总数（运行中 + 历史），悬停显示构成

## v1.0.4 (2026-09-13)

- 顶栏合并为单行（Logo + 拾忆 + 副标 + 导航 + 状态），新增专注模式 ⌘⇧F
- 新建会话默认目录改为用户主目录并记忆上次目录
- 抽屉交互完善（历史折叠沉底 / 运行中铺满 / 拖拽调宽）
- tmux 自动探测，创建前校验 tmux 与 CLI 依赖

## v1.0.3 (2026-09-13)

- 移除全部本机写死路径：新会话默认目录为用户主目录，并记住上次使用目录
- 发布包不再携带本机路径映射（仅保留通用示例）

## v1.0.2 (2026-09-13)

- tmux 自动探测（兼容 Homebrew 不同路径 / 登录 shell PATH），未安装时给出明确提示
- 创建会话前置检查：未安装 Claude Code / Codex CLI 时提示对应安装命令
- 修复内置终端打开接口的 await 时序问题
- 错误提示高对比、可换行，不再被模糊背景影响
- 抽屉：历史折叠时运行中铺满、折叠栏沉底

## v1.0.1 (2026-09-13)

- 修复二进制部署目标过高导致的兼容问题：最低支持 macOS 12（此前在 macOS 26 等系统提示"不能与此版本 macOS 配合使用"）
- 终端选中效果、抽屉拖拽调宽、历史操作收敛、打开会话目录等一系列体验改进

## v1.0.0 (2026-09-07)

- 会话：运行窗口 / tmux 实时映射、历史检索、一键恢复、新建与终止
- 内置终端：tmux + xterm.js 工作台（多标签、自适应、清屏、复制粘贴）
- 技能：全局 / 项目两级浏览与管理
- 配置：规则 / MCP（连通性检测）/ Agent / 命令 / Hooks / 设置文件编辑器
- macOS 原生壳（Swift + WebKit）、明亮主题、简笔画 Logo
- 路径别名与 Codex 会话迁移工具
